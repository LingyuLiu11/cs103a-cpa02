<p>Result of books</p>
<table class="table table-bordered table-striped">
  <tbody>
    <% for (book of books ){%>
    <tr>
      <td><%= book.name %></td>
      <td><%= book.author %></td>
      <td><%= book.price %></td>
    </tr>
    <% } %>
  </tbody>
</table>
